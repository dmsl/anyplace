export class radioPoint {

    constructor(x: number,  y: number, macList:number, countPoints:number) { // macList:Array<number>) {
        this.x = x;
        this.y = y;
        this.macList = macList;
        this.countPoints = countPoints;
    }
    x: number;
    y: number;
    macList:number;
    countPoints:number;
    // macList:Array<number>;

}