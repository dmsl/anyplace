FROM couchbase:community-6.0.0

ARG DIR
ARG ROOT_USER
ARG ROOT_PASS
ARG USER
ARG PASS
ARG CLUSTER
ARG BUCKET
ARG RAM
ARG RAM_INDEX
ARG RAM_BUCKET

# INFO the initialization of couchbase is done with post_install.sh
# This is essentially adds nothing more to the official 6.0 community edition

#RUN "/opt/couchbase/configure.sh" $ROOT_USER $ROOT_PASS $USER $PASS $CLUSTER $BUCKET $RAM $RAM_INDEX $RAM_BUCKET
