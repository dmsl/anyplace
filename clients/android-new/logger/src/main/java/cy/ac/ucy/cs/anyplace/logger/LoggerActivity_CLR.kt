package cy.ac.ucy.cs.anyplace.logger

class LoggerActivity_CLR {




  // CLR
  // private fun checkLocationPermission() {
  //   LOG.E("checkLocationPermission(): TODO:CLR")
  //
  //   PermUtils.requestPermission(this,
  //           Manifest.permission.ACCESS_FINE_LOCATION,
  //           "Location Permission",
  //           //, please accept to use location functionality
  //           "Fine-grain location permission is required"
  //   )
  //
  //   PermUtils.requestPermission(this,
  //           Manifest.permission.ACCESS_COARSE_LOCATION,
  //           "Location Permission",
  //           "Coarse-grain location permission is required"
  //   )
  // if (ActivityCompat.checkSelfPermission(this, Manifest.permission.ACCESS_FINE_LOCATION)
  //         != PackageManager.PERMISSION_GRANTED) {
  //
  //   // Should we show an explanation?
  //   if (ActivityCompat.shouldShowRequestPermissionRationale(this,
  //                   Manifest.permission.ACCESS_FINE_LOCATION)) {
  //
  //     // Show an explanation to the user *asynchronously* -- don't block
  //     // this thread waiting for the user's response! After the user
  //     // sees the explanation, try again to request the permission.
  //     AlertDialog.Builder(this)
  //             .setTitle("Location Permission Needed")
  //             .setMessage("This app needs the Location permission, please accept to use location functionality")
  //             .setPositiveButton("OK") { dialogInterface, i -> //Prompt the user once explanation has been shown
  //               ActivityCompat.requestPermissions(this@AnyplaceLoggerActivity, arrayOf(Manifest.permission.ACCESS_FINE_LOCATION),
  //                       MY_PERMISSIONS_REQUEST_LOCATION)
  //             }
  //             .create()
  //             .show()
  //   } else {
  //     // No explanation needed, we can request the permission.
  //     ActivityCompat.requestPermissions(this, arrayOf(Manifest.permission.ACCESS_FINE_LOCATION),
  //             MY_PERMISSIONS_REQUEST_LOCATION)
  //   }
  // }
  // if (ActivityCompat.checkSelfPermission(this, Manifest.permission.ACCESS_COARSE_LOCATION)
  //         != PackageManager.PERMISSION_GRANTED) {
  //
  //   // Should we show an explanation?
  //   if (ActivityCompat.shouldShowRequestPermissionRationale(this,
  //                   Manifest.permission.ACCESS_COARSE_LOCATION)) {
  //
  //     // Show an explanation to the user *asynchronously* -- don't block
  //     // this thread waiting for the user's response! After the user
  //     // sees the explanation, try again to request the permission.
  //     AlertDialog.Builder(this)
  //             .setTitle("Location Permission Needed")
  //             .setMessage("This app needs the Location permission, please accept to use location functionality")
  //             .setPositiveButton("OK") { dialogInterface, i -> //Prompt the user once explanation has been shown
  //               ActivityCompat.requestPermissions(this@AnyplaceLoggerActivity, arrayOf(Manifest.permission.ACCESS_COARSE_LOCATION),
  //                       MY_PERMISSIONS_REQUEST_LOCATION)
  //             }
  //             .create()
  //             .show()
  //   } else {
  //     // No explanation needed, we can request the permission.
  //     ActivityCompat.requestPermissions(this, arrayOf(Manifest.permission.ACCESS_COARSE_LOCATION),
  //             MY_PERMISSIONS_REQUEST_LOCATION)
  //   }
  // }
  // }



  // override fun onStart() {   super.onStart() }
  // override fun onStop() {
  //   super.onStop()
  //   // Disconnecting the client invalidates it.
  // }




  // GLOBALS:
  // Define a request code to send to Google Play services This code is
  // returned in Activity.onActivityResult


//   IN ON CREATE:

  // override fun onStart() {   super.onStart() }
  // override fun onStop() {
  //   super.onStop()
  //   // Disconnecting the client invalidates it.
  // }
}